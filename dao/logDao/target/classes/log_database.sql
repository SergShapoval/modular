CREATE SCHEMA IF NOT EXISTS modular;
USE modular;
CREATE TABLE IF NOT EXISTS log(
  id INT AUTO_INCREMENT NOT NULL PRIMARY KEY,
  logger_level VARCHAR (10) NOT NULL DEFAULT '',
  logger_info VARCHAR (500) NOT NULL DEFAULT ''
);